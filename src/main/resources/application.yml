logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
spring:
  datasource:
    driverClassName: org.h2.Driver
    password: ''
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
  h2:
    console:
      enabled: true
      path: /h2_console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
